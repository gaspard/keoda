import * as _ from '..'

export const colors = _.card('colors', {
  writ: 'colors',
  desc: () => `
# Colors

* ${_.pan} (red)
* ${_.eri} (cherry)
* ${_.pon} (orange)
* ${_.piu} (green)
* ${_.fio} (eggplant)
* ${_.wi} (prune)
`,
})
