import * as _ from '../lang'
import { prefix } from '../zulapa'

export const go = prefix('go', {
  pref: 'big',
  adj: 'intense',
  glo: 'AUG',
  force: 'noun',
  etym: () => [_.gu, _.o$],
  see: () => [_.gi],
})
