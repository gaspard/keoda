import * as _ from '../lang'

export const shaH = _.word('sha', {
  id: 'shaD',
  noun: 'hip',
  see: () => [_.sha],
})

export const shany = _.word('shany', {
  noun: 'dance',
  verb: 'to dance',
  etym: () => [_.shaH, _.y$],
})

// === alt
