import * as _ from '../lang'

export const elem = _.word('elem', {
  noun: 'in the past',
  etym: () => [_.e, _.em],
})

export const lemana = _.word('lemana', {
  noun: 'history',
  etym: () => [_.em, _.na],
})

// === alt
