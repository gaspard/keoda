import * as _ from '../lang'

export const emle = _.word('emle', {
  noun: 'in the past',
  etym: () => [_.em, _.e],
})

export const emlena = _.word('emlena', {
  noun: 'history',
  etym: () => [_.em, _.na],
})

// === alt
