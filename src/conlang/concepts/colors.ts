import * as _ from '../words'

export const colors = _.card('colors', {
  writ: 'colors',
  desc: () => `
# Colors

* ${_.adjRef(_.pan)}
* ${_.adjRef(_.eri)}
* ${_.adjRef(_.pon)}
* ${_.adjRef(_.piu)}
* ${_.adjRef(_.fio)}
* ${_.adjRef(_.wi)}

`,
})
