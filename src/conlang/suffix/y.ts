import { suffix } from '../zulapa'

export const y = suffix('y', {
  id: 'y$',
  verb: 'to dance',
  glo: '*DANCE*',
  force: 'verb',
  desc: () => `Moving without a direction, drawing, dancing.`,
})
