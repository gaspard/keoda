import * as _ from '../words'

export const nur = _.word('nur', {
  adj: 'only',
  exam: () => [
    _.phrase(
      'I am just a spark of consciousness.',
      _.es.o.lu.e.nur,
      _.aj.lil.wu,
      _.kegai
    ),
  ],
})
