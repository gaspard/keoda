/*
 */
import * as _ from '.'

export const seasons = _.example(
  [
    // Endive (janvier)
    _.egina,
    // Chou (février)
    _.fena,
    // Poireaux (mars)
    _.piutaj,
    // Asperge (avril)
    _.ulul,
    // Epinards (mai)
    _.piuna,
    // Cerise (juin)
    _.erina,
    // Abricot (juillet)
    _.palajil,
    // Tomate (août)
    _.putam,
    // Pruneau (septembre)
    _.wiyon,
    // Courge (octobre)
    _.gola,
    // Orange (novembre)
    _.pona,
    // Mandarine (décembre)
    _.ponalil,
  ],
  ``
)
