/*
 */
import * as _ from '.'

export const seasons = _.example(
  [
    // Endive (janvier)
    // Chou (février)
    // Poireaux (mars)
    _.piutaj,
    // Asperge (avril)
    _.ulul,
    // Epinards (mai)
    _.piuna,
    // Cerise (juin)
    // Abricot (juillet)
    _.palajil,
    // Tomate (août)
    // Pruneau (septembre)
    _.wiyon,
    // Courge (octobre)
    // Orange (novembre)
    _.pona,
    // Mandarine (décembre)
    _.ponalil,
  ],
  ``
)
