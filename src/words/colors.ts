import * as _ from '.'

export const colors = _.example(
  [
    // Rouge
    _.pan,
    // Orange
    _.pon,
    // Vert
    _.piu,
    // Aubergine
    _.fio,
    // Pruneau (violet)
    _.wi,
  ],
  ''
)
