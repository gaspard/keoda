import * as _ from '.'

export const scale = _.example(
  [
    // 10 (souvenir)
    _.djipa,
    // 9 (joie)
    _.dapa,
    // 8 (appartenance)
    _.jepa,
    // 7 (fierté)
    _.fepa,
    // 6 (croyance)
    _.shipa,
    // 5 (confusion)
    _.nelil,
    // 4 (honte)
    _.neka,
    // 3 (oubli)
    _.nepe,
    // 2 (stress)
    _.nede,
    // 1 (division)
    _.nepi,
  ],
  'échelle du néant (nepa)'
)
