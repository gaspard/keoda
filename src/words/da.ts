import * as _ from '.'

export const da = _.word('da', { noun: 'lieu, 9', suffix: 'lieu' })
_.see(_.counting)

_.see(_.lama)
_.see(_.lamagoa)
_.see(_.goa)

export const dapa = _.word('dapa', {
  noun: 'joie, émotion',
})
